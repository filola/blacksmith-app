# 🔄 개발 워크플로우 (v0.6.0+ 필수)

## ⚠️ 문제 기록 (2026-02-17)
- **반복되는 버그**: 매 배포마다 한글 깨짐, 제련탭 재출현, 파일 누락
- **근본 원인**: main 브랜치와 feature 브랜치 동기화 실패, merge 불완전
- **교훈**: 정확한 버전 관리 및 완전한 merge 확인 필수!

---

## 📋 필수 워크플로우 (절대 변경 금지!)

### 1️⃣ 새 작업 시작
```bash
# main 최신 상태 확인
git checkout main
git pull origin main

# v0.6.Y-feature 브랜치 생성 (Y = 마이너 버전)
git checkout -b v0.6.Y-feature
git push origin v0.6.Y-feature
```

### 2️⃣ 작업 진행
- feature 브랜치에서만 작업
- 완료 후 local 테스트 (가능하면)
- 커밋 메시지: `feat:` `fix:` `refactor:` 등으로 명확하게

### 3️⃣ Main으로 merge (완전 동기화 필수!)
```bash
# v0.6.Y-feature에서 작업 완료
git add -A
git commit -m "작업 설명"
git push origin v0.6.Y-feature

# main 브랜치로 전환
git checkout main
git pull origin main

# merge 수행
git merge v0.6.Y-feature

# 🔴 반드시 확인! (완전 merge 검증)
git diff HEAD v0.6.Y-feature  # 차이 없어야 함!
ls -la scenes/skill_tree_tab.* scripts/skill_tree_tab.*  # 중요 파일 존재?
grep -r "[가-힣]" scenes/ scripts/  # 한글 남았나?

# 모든 파일이 정확하면 push
git push origin main
```

### 4️⃣ 버전 태그 생성 및 배포
```bash
# 태그 생성 (반드시 정확한 버전으로)
git tag v0.6.Y
git push origin v0.6.Y

# GitHub Actions 자동 배포 시작
# (main push → Actions trigger → gh-pages 배포)
```

### 5️⃣ 배포 검증
```
URL: https://filola.github.io/blacksmith-app/?v=0.6.Y

캐시 무시:
- 시크릿 모드 또는
- 강제 새로고침 (Cmd+Shift+R)
```

---

## 🚨 주의사항

### 절대 금지!
1. ❌ feature 브랜치 건너뛰고 직접 main에서 작업
2. ❌ main에 push 후 tag 안 생성
3. ❌ merge 후 파일 누락 확인 안 함
4. ❌ 한글 남은 상태로 배포

### 필수 검증 (merge 후 반드시!)
```bash
# 1. 차이 없는지 확인
git diff HEAD origin/v0.6.Y-feature

# 2. 중요 파일 존재하는지 확인
find . -name "*.gd" -o -name "*.tscn" | grep skill_tree

# 3. 한글이 남아있지 않는지 확인
grep -r "[가-힣]" scenes/ scripts/ autoload/

# 4. main과 feature 커밋 로그 동일한지 확인
git log main --oneline -1
git log v0.6.Y-feature --oneline -1
# → 커밋 해시가 같아야 함!
```

---

## 📊 버전 관리 예시

```
v0.6.0 ✅ (2026-02-17 11:32) - 스킬트리 시스템 추가
  └─ issue: 한글 깨짐

v0.6.1-feature (다음 작업)
  - 한글 깨짐 수정
  - GameManager 스킬 로드 로직 검증
  - main에 완전 merge
  └─ v0.6.1 tag + 배포

v0.6.2-feature
  - [다음 기능]
  - main에 완전 merge
  └─ v0.6.2 tag + 배포
```

---

## 💡 팁

**merge가 불완전해 보이면:**
```bash
# 강제로 동기화
git reset --hard v0.6.Y-feature
git push origin main --force
git tag -d v0.Y.Z  # 기존 태그 삭제
git tag v0.Y.Z
git push origin v0.Y.Z --force
```

**GitHub Actions 배포가 안 되면:**
- main에 push 되었는지 확인
- GitHub Actions logs 확인 (Actions 탭)
- force push가 필요할 수 있음

---

## 🎯 최종 체크리스트

매 배포 전에:
- [ ] v0.X.Y-feature 브랜치에서 작업했나?
- [ ] main으로 merge했나?
- [ ] merge 후 파일 누락 없는지 확인했나?
- [ ] 한글이 남아있지 않은지 확인했나?
- [ ] v0.X.Y 태그 생성했나?
- [ ] origin에 push했나?
- [ ] GitHub Actions 배포 성공했나?
- [ ] 라이브 사이트에서 ?v=X.Y.Z로 확인했나?

모두 ✅ 완료 후에만 다음 작업 시작!

---

**작성**: 2026-02-17
**수정 금지**: 이 워크플로우는 **절대** 변경하지 말 것
**준수 방법**: Opus 작업 시작 전에 **반드시** 이 파일을 읽고 따를 것
